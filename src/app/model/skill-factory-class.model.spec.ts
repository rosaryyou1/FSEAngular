import { SkillFactoryClass } from './skill-factory-class.model';

describe('SkillFactoryClass', () => {
  it('should create an instance', () => {
    expect(new SkillFactoryClass()).toBeTruthy();
  });
});
